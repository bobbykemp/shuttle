package com.rjkemp.shuttle;

import java.awt.*;
import javax.swing.*;

public class LocalTransfer extends JPanel {
    public LocalTransfer() {
        super(new BorderLayout());
        JLabel label = new JLabel("Local Transfer Settings");
        add(label);
    }
}
