package com.rjkemp.shuttle;

import java.awt.*;
import javax.swing.*;

public class RemoteTransfer extends JPanel {
    public RemoteTransfer() {
        super(new BorderLayout());
        JLabel label = new JLabel("Remote Tranasfer Settings");
        add(label);
    }
}
